#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: the secong number to multiply with first number
 * Return: The results of mutiplication  between x and y
 */

int mul(int x, int y)

{
	return (x * y);
}
